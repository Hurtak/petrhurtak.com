language: node_js

# Build matrix

os:
  - linux
  - osx

node_js:
  - "node" # latest node
  - "7" # latest 7.x.x node
  - "6"

# Build lifecycle

install:
  - npm install

script:
  - npm run lint
  - npm run test+coverage

after_success:
  - npm run coveralls
