<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">
  <channel>

    <title>{{ siteDomain }}</title>
    <link>{{ siteUrl }}</link>
    <description>Blog about web design and technologies</description>

    {% for article in articles %}
      <item>
        <title>{{ article.title }}</title>
        <link>{{ siteUrl }}/{{ article.url }}</link>
        <guid>{{ siteUrl }}/{{ article.url }}</guid>
        <pubDate>{{ article.publicationDate | gmt }}</pubDate>
        <description>{{ article.description }}</description>
      </item>
    {% endfor %}

  </channel>
</rss>
