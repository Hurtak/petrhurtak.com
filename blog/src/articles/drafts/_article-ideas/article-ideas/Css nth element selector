
        // everything except first column
        &:nth-child(@{items}n+1) {
            margin-left: 0;
        }

        // first row
        &:nth-child(-n+@{items}) {
            margin-top: 0;
        }
