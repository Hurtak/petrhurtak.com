
What it names of the day shortened and full,
What are their indexes in JS
The same thing for months
getUTCdate (všechny funkce mají UTC variantu)
Vs
getDate


    const dayNames = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']
    const monthNames = [
      'Jan', 'Feb', 'Mar', 'Apr',
      'May', 'Jun', 'Jul', 'Aug',
      'Sep', 'Oct', 'Nov', 'Dec'
    ]


    year: date.getFullYear(),
    month: addLeadingZero(date.getMonth() + 1),
    day: addLeadingZero(date.getDate()),
    dayOfWeek: date.getDay(),
    hours: addLeadingZero(date.getHours()),
    minutes: addLeadingZero(date.getMinutes()),
    seconds: addLeadingZero(date.getSeconds())





  const months = [
    'January',
    'February',
    'March',
    'April',
    'May',
    'June',
    'July',
    'August',
    'September',
    'October',
    'November',
    'December'
  ]
