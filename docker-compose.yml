version: '2.1'

services:
  blog-app:
    container_name: blog-app
    build:
      context: .
      dockerfile: Dockerfile.app
    ports:
      - "8000:8000"
    # restart: always
    volumes:
      - ./src:/usr/src/app/src
      - ./scripts:/usr/src/app/scripts
      - ./articles:/usr/src/app/articles
    environment:
      NODE_ENV: development

  blog-db:
    container_name: blog-db
    build:
      context: .
      dockerfile: Dockerfile.db
    # volumes_from:
    #   - blog-db-data
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: hurtak_blog

  # blog-db-data:
  #   container_name: blog-db-data
  #   image: tianon/true
  #   volumes:
  #     - /var/lib/mysql
